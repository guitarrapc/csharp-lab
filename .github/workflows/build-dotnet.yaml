name: Build Dotnet

on:
  workflow_dispatch:
  schedule:
    - cron: 0 0 * * 0 # At 00:00 on Sunday.
  push:
    branches:
      - "main"
  pull_request:
    branches:
      - "main"

jobs:
  build:
    uses: ./.github/workflows/_reusable-build.yaml
